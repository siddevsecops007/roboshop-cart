name: Cart CI Pipeline
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  tests:
    runs-on: self-hosted
    needs: build
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  DEV:
    runs-on: self-hosted
    needs: tests
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo
