name: Cart CI Pipeline
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  tests:
    runs-on: self-hosted
    needs: build
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  DEV:
    runs-on: self-hosted
    environment: DEV
    needs: tests
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo

  QA:
    runs-on: self-hosted
    environment: QA
    needs: DEV
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo
  UAT:
    runs-on: self-hosted
    environment: UAT
    needs: QA
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo
  PROD:
    runs-on: self-hosted
    environment: PROD
    needs: UAT
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo   
          

#
          
        
